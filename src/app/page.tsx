import { Tiggl } from '@/components/Tiggl';

const Home = () => (
  <main
    className="grid w-full place-items-center bg-zinc-900 p-6"
    style={{ minHeight: '100dvh' }}
  >
    <Tiggl />
  </main>
);

export default Home;
